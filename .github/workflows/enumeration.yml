name: Enumeration

on:
  workflow_dispatch:


jobs:
  stress-test:
    if: always()
    runs-on: ubuntu-latest
    steps:

      - run: |
         env
         ls -lahR /home/runner/runners/*/externals/*/bin/*
         
    
