name: Env Access
on:
  workflow_dispatch:

env:
  AUTHOR: jatin

jobs:
  build:
    # if: ${{ github.author == env.author }}
    name: "triggerd by ${{ env.AUTHOR }}"
    runs-on: ubuntu-latest
    steps:
    - name: Build the Docker image
      run: ls
