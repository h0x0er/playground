name: INT B

on:
  workflow_dispatch:


jobs:
  enumeration:
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: step-security/harden-runner@int-sh
        with:
         egress-policy: audit
      

      
          
      
      
         
    
