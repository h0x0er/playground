name: INT B

on:
  workflow_dispatch:


jobs:
  enumeration:
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: h0x0er/harden-runner@debug2
        with:
         egress-policy: audit
      

      
          
      
      
         
    
